<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <title>Bootstrap with Twitter Feed</title>
</head>


<body>
<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{ path('home') }}">
            <img src="{{ asset('image/logoImage.png') }}" alt="Example Image" width="50em" height="50em">
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link active" aria-current="page" href="{{ path('home') }}">Page d'accueil</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('products.index') }}">Acheter</a>
                </li>


            </ul>

        </div>
    </div>

    {% if app.user %}
        <div class="mb-3 col-2 d-flex align-items-center">
            <div style="margin-right: 1rem">
                {{ app.user.userIdentifier }}
            </div>
            <a href="{{ path('admin_product_all') }}" style="margin-right: 1rem">
                <img src="{{ asset('image/admin.png')}}"  width="30rem" height="30rem" />
            </a>
            <a href="{{ path('app_logout') }}" style="color: red; margin-right: 1rem" >
                <img src="{{ asset('image/logout.png') }}" width="30rem" height="30rem" />
            </a>
        </div>
    {% else %}
        <div class="mb-3 col-1 ">
            <a href="{{ path('app_login') }}" style="color: red">
                <img src="{{ asset('image/login.png') }}" width="30rem" height="30rem" />
            </a>
        </div>
    {% endif %}

</nav>
{% block body %}{% endblock %}

<script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.2/dist/umd/popper.min.js"></script>
<script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>




</html>