{% extends 'base.html.twig' %}


{% block body %}
    <div class="container mt-4">
        <div class="jumbotron">
            <div class="row">
                <div class="col-md-8">
                    {#                ajouter une image ici#}
                </div>
                <div class="col-md-4">
                    <h1>{{ product.title }}</h1>
                    <div class="text-primary" style="font-size: 2rem; font-weight: bold">{{ product.formattedPrice }} €</div>
                    <p>
                        {{ product.description }}
                    </p>
                    <a href="#" class="btn btn-primary">+</a>
                    <input type="text" style="width: ">
                    <a href="#" class="btn btn-primary">-</a>
                    <br/> <br/>
                    <a href="#" class="btn btn-primary">Ajouter au panier</a>
                </div>

            </div>
        </div>

        Fiche des informations supplémentaires
        <div class="row">
            <div class="col-md-8">
                <h2>Caractéristiques</h2>
                <table class="table table-striped">
                    <tr>
                        <td>Marchand</td>
                        <td>{{ product.merchant }}</td>
                    </tr>
                    <tr>
                        <td>Poids</td>
                        <td>{{ product.weight }} KG</td>
                    </tr>
                    <tr>
                        <td>Catégorie</td>
                        <td>{{ product.category }}</td>
                    </tr>
                </table>
            </div>
            <div class="col-md-4">
                <h2>Spécificité</h2>
            </div>
        </div>
    </div>
{% endblock %}
